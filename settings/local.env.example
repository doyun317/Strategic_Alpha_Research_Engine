# Copy to local.env and customize non-secret local overrides.
# LLM-specific values belong in llm.env.

SAE_ENV=development
SAE_LOG_LEVEL=DEBUG
SAE_REGION=USA
SAE_UNIVERSE=TOP3000
SAE_DEFAULT_TEST_PERIOD=P1Y0M0D
