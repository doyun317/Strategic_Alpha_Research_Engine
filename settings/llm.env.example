# Copy to llm.env and customize for local/private LLM access.
# `autopilot` requires a structured JSON-capable LLM endpoint.
# Increase timeout if agenda/planner/critic calls are slow.

SAE_LLM_BASE_URL=http://127.0.0.1:8000/v1
SAE_LLM_MODEL=example-model
SAE_LLM_TIMEOUT_SECONDS=30
